__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
.DS_Store
# Never commit the local session/credentials (also lives in ~/.config), nor
# anything downloaded from a real account.
config.json
*.pdf
